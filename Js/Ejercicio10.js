/*Acceder a un Array*/
let numeros = [1, 2, 3, 4, 5];

// Modificar el tercer valor
numeros[2] = 100;  // Cambia el valor en el índice 2 (tercer elemento)

// Imprimir el array completo
console.log(numeros);
